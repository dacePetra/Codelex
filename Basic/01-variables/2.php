<?php

$number = 10;

$floatNumber = number_format(10.1,2);

$greeting = "hello world";

var_dump($number);
var_dump($floatNumber);
var_dump($greeting);

//Exercise 2
//Dump the same values that should display both data type & its value.
// (Note, usage of var_dump)